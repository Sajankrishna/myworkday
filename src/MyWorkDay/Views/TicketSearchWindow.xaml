<Window x:Class="MyWorkDay.Views.TicketSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find a ticket" Width="480" Height="540" MinWidth="380" MinHeight="360"
        WindowStartupLocation="CenterOwner" Background="{StaticResource PageBg}"
        FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QueryBox" Grid.Column="0" Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                     Padding="9" Margin="0,0,8,0" FontSize="14" KeyDown="QueryBox_KeyDown"/>
            <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="Search" Command="{Binding SearchCommand}"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Results}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="{StaticResource Line}" BorderThickness="1" CornerRadius="10" Padding="10" Margin="0,0,0,8"
                                    Cursor="Hand" MouseLeftButtonUp="Result_Click" Tag="{Binding}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Key}" FontFamily="Consolas" FontWeight="Bold" Foreground="{StaticResource Blue}" FontSize="12.5"/>
                                        <Border Style="{StaticResource StatusPill}" Margin="8,0,0,0" Visibility="{Binding Status, Converter={StaticResource NullToVis}}">
                                            <TextBlock Text="{Binding Status}" Style="{StaticResource StatusPillText}"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Summary}" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="No matching tickets." HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource MutedText}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Searched}" Value="True"/>
                                        <Condition Binding="{Binding Results.Count}" Value="0"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>

<Window x:Class="MyWorkDay.Views.TeamWorklogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MyWorkDay.ViewModels"
        Title="Team Worklogs - MyWorkDay" Width="620" Height="700" MinWidth="480" MinHeight="420"
        Background="{StaticResource PageBg}" FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Team Worklogs" FontSize="16" FontWeight="Bold"/>
                <TextBlock Text="{Binding DateLabel}" Style="{StaticResource MutedText}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <DatePicker SelectedDateChanged="DatePicker_Changed" x:Name="DatePickerCtl" Width="130" Margin="0,0,8,0"/>
                <Button Style="{StaticResource ActionButton}" Content="&#8635; Refresh" Command="{Binding RefreshCurrentCommand}"/>
            </StackPanel>
        </Grid>

        <Border Style="{StaticResource CardStyle}" Grid.Row="1" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Add a teammate" Style="{StaticResource CardHeaderText}" Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Padding="7" Margin="0,0,8,0" KeyDown="SearchBox_KeyDown"/>
                    <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="Search" Command="{Binding SearchCommand}"/>
                </Grid>
                <ItemsControl ItemsSource="{Binding SearchResults}" Margin="0,8,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource PageBg}" CornerRadius="8" Padding="8" Margin="0,0,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="12.5"/>
                                        <TextBlock Text="{Binding Email}" FontSize="11" Foreground="{StaticResource Muted}"/>
                                    </StackPanel>
                                    <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="+ Add"
                                            Click="AddMember_Click" Tag="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Row="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Your team" Style="{StaticResource CardHeaderText}" Margin="0,0,0,8"/>
                    <ItemsControl ItemsSource="{Binding Members}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:TeamMemberRowViewModel}">
                                <Border BorderBrush="{StaticResource Line}" BorderThickness="1" CornerRadius="10" Margin="0,0,0,8">
                                    <StackPanel>
                                        <Grid Margin="10" Cursor="Hand" MouseLeftButtonUp="MemberRow_Toggle">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="12.5"/>
                                                <TextBlock Text="{Binding Email}" FontSize="11" Foreground="{StaticResource Muted}"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding TotalLabel}" FontWeight="Bold" Foreground="{StaticResource Blue}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <Button Grid.Column="2" Content="&#10005;" Padding="6,2" Background="Transparent" BorderThickness="0"
                                                    Foreground="{StaticResource Red}" Click="RemoveMember_Click" Tag="{Binding}"/>
                                        </Grid>
                                        <ItemsControl ItemsSource="{Binding Tickets}" Margin="10,0,10,10"
                                                      Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,3">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="70"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{Binding Key}" FontWeight="SemiBold" Foreground="{StaticResource Blue}" FontSize="11.5"/>
                                                        <TextBlock Grid.Column="1" Text="{Binding Summary}" FontSize="11.5" Foreground="{StaticResource Muted}" TextTrimming="CharacterEllipsis" Margin="6,0"/>
                                                        <TextBlock Grid.Column="2" FontWeight="SemiBold" FontSize="11.5">
                                                            <Run Text="{Binding Minutes, Mode=OneWay}"/>
                                                            <Run Text="m"/>
                                                        </TextBlock>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="No teammates yet - search above to add one." Style="{StaticResource MutedText}"
                               Visibility="{Binding Members.Count, Converter={StaticResource ZeroToVis}, ConverterParameter=Invert}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
